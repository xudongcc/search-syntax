/**
 * Logical connective types used for combining search conditions.
 */
export enum Connective {
  /** Logical AND - all conditions must match */
  AND = "AND",
  /** Logical OR - at least one condition must match */
  OR = "OR",
}
